# Node Modules (Root)
/node_modules

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor specific files
.idea/
.vscode/
*.swp
*~
*.sublime-project
*.sublime-workspace

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Build / Dist outputs (if any at root)
/dist
/build

# Environment variables (if any at root, though they should be in subdirs)
.env*
!.env.example

# Specific Env files
backend/.env
frontend/.env

# Other
.npm
.yarn-integrity
.pnp.*

# Expo web build output
frontend/dist/

# Deployment configs (if not intended to be committed)
# app.yaml 
# digitalocean_app_spec.txt
# railway.toml # Keep this one usually
