// Token expiry in seconds (60 seconds = 1 minute)
export const TOKEN_EXPIRY_SECONDS = 60;

// Room expiry in seconds (7200 seconds = 2 hours)
export const ROOM_EXPIRY_SECONDS = 7200;

// Default language if not specified
export const DEFAULT_LANGUAGE = 'en';

// Maximum message length
export const MAX_MESSAGE_LENGTH = 1000;
